function isSun(num) {
    if (num > 5) {
        return true;
    }
    else {
        return false;
    }
}


// step two

const sum  = (a,b)  => a+b;
const res  =(4,2);
console.log(res)

const num = isSun(1);
console.log(num);